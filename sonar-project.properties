# must be unique in a given SonarQube instance
sonar.projectKey=ultron-core

# this is the name displayed in the SonarQube UI
sonar.projectName=Ultron Core
sonar.projectVersion=6.1

# Path is relative to the sonar-project.properties file. Replace  by  on Windows.
# Since SonarQube 4.2, this property is optional if sonar.modules is set. 
# If not set, SonarQube starts looking for source code from the directory containing 
# the sonar-project.properties file.

sonar.sources=app,database,resources
sonar.exclusions=resources/assets/**,storage/**,vendor/**,app/Console/**,app/Events/**,app/Http/routes.php,app/Libraries/**,resources/**/*blade.php,resources/lang/**

sonar.tests=tests

# Reusing PHPUnit reports
sonar.php.coverage.reportPath=public/coverage/codeCoverage.xml
sonar.php.tests.reportPath=public/coverage/logfile.xml

# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8
